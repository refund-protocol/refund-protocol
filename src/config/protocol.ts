/**
 * Protocol-level configuration.
 * Values are illustrative and non-operational.
 */

export const PROTOCOL_VERSION = "0.1.0";

export const ELIGIBILITY_RULESET_VERSION = "2025-01";

export const BATCH_LIMITS = {
  maxClaimsPerBatch: 500,
  minBatchIntervalSeconds: 3600,
};
